<div style="max-width:800px;margin:0 auto;padding:5rem 2.5rem">
  <div style="font-size:0.65rem;letter-spacing:4px;text-transform:uppercase;color:#c9a96e;margin-bottom:1rem">{{ page.title }}</div>
  <h1 style="font-family:'Playfair Display',serif;font-size:2.5rem;color:#f0ebe3;margin-bottom:2rem">{{ page.title }}</h1>
  <div style="font-size:0.9rem;line-height:1.9;color:#7a756f;font-weight:300">{{ page.content }}</div>
</div>
